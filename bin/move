#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = []
9.times do
  board << " "
end
puts "Welcome to Tic Tac Toe!"
s = gets "Where would you like to go?"
num  = input_to_index(s)
move(board,num)
display_board(board)
