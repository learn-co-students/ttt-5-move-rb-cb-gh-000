#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
puts "Welcome to Tic Tac Toe!"

#ask for user input
input = gets.strip
index = input_to_index(input)
move(board, index)

display_board(board)
