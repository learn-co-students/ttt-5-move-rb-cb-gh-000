#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"

puts "Where would you like to go?"

input = gets.strip
input_to_index(input)

index = input
## expect self to recieve move.with(anything,0,(any_args))


move(board,0,"X")

display_board(board)


#make sure bin/move is passing index, not the input to the move method
