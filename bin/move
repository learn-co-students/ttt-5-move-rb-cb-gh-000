#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = Array.new(9, " ")

puts "Welcome to Tic Tac Toe!"
# Code your CLI Here

#until game is over: put loop here
puts "Where would you like to go?"
input = gets.strip
index = input_to_index(input)
move(board, index)

display_board(board)
