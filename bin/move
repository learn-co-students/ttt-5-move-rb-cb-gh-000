#!/usr/bin/env ruby

require_relative '../lib/move.rb'

board = Array.new(9,' ')

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"

player_input = input_to_index(gets)

move(board, player_input)

display_board(board)
